const fs = require('fs');
let code = fs.readFileSync('src/lib/assistant.functions.ts', 'utf-8');

const target = `ПРОФЕСИОНАЛНИ ПРАВИЛА ЗА ВАЙРЪЛ РЕЖИСУРА (PRO WORKFLOW):
1. СТРИКТНО ПРАВИЛО ЗА ТЕМИТЕ: Търси ДЪЛБОКИ, ВЪЗДЕЙСТВАЩИ, ПО-РЯДКО ЦИТИРАНИ уроци. НЕ ПОВТАРЯЙ теми, които вече са в history.
Всяка тема трябва да има СИЛНА КУКА (Viral Hook) в първите 3 секунди (напр. "Знакът, че Аллах е чул молитвата ти...").
За всяка тема ВИНАГИ задавай "useBRoll": true, "bRollInterval": 5.

SALAFI HALAL ПРИНЦИПИ (СТРИКТНО ЗАДЪЛЖИТЕЛНО):`;

const replacement = `ПРОФЕСИОНАЛНИ ПРАВИЛА ЗА ВАЙРЪЛ РЕЖИСУРА (PRO WORKFLOW):
1. СТРИКТНО ПРАВИЛО ЗА ТЕМИТЕ И ДАЛИЛ (Доказателство): Избирай теми, които решават РЕАЛНИ проблеми на хората и това, което търсят най-много (напр. стрес, дългове, липса на съпруг/а, търпение при трудности, депресия, токсични хора).
ЗАДЪЛЖИТЕЛНО е във всяко видео или карусел да предоставяш ясен ДАЛИЛ (доказателство) - точен Аят от Корана или достоверен Хадис (Бухари, Муслим и др.), който дава решението на този проблем. 
Всяка тема трябва да има СИЛНА КУКА (Viral Hook) в първите 3 секунди (напр. "Знакът, че Аллах е чул молитвата ти...").
За всяка тема ВИНАГИ задавай "useBRoll": true, "bRollInterval": 5.

SALAFI HALAL ПРИНЦИПИ (СТРИКТНО ЗАДЪЛЖИТЕЛНО):
Всяко съдържание (текст, хадиси, уроци) трябва да съответства строго на Салафитската методология (Quran & Sunnah upon the understanding of the Salaf). Без бида (нововъведения), без слаби (da'if) хадиси.`;

if (code.includes(target)) {
  code = code.replace(target, replacement);
  fs.writeFileSync('src/lib/assistant.functions.ts', code);
  console.log("Successfully updated PRO WORKFLOW instructions.");
} else {
  console.log("Error: Target string not found.");
}
